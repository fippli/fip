// Test object destructuring - shorthand
{ name, age }: { name: "John", age: 30 }

log!(name)
log!(age)

// Test nested object destructuring
{ name: { first-name }}: { name: { first-name: "John", last-name: "Doe" } }

log!(first-name)

// Test the user's example (corrected)
{ name: { age-value }}: { name: { age-value: 32 } }

log!(age-value)

