# Comments

Use `//` to start a single-line comment. Everything from the slashes to the end of the line is ignored by the lexer.

```
// This line does nothing
log!("visible output") // trailing comments work too
```
